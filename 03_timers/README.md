# Exercise 3 

This program reads commands from the serial port using an interrupt-driven UART driver. It manages an inactivity timer that resets received input after 30 seconds, toggles an LED, and supports simple commands like help, interval <number>, and time
